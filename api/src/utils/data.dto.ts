export interface CreateDTO {
  name: string;
  message: string;
  latitude: string;
  longitude: string;
}

export interface ResDTO {
  _id: string;
  name: string;
  message: string;
  latitude: string;
  longitude: string;
  date: string;
}
